from enum import Enum
import enum

"""renpy
init python:
"""


class V6_Relics(Enum):
    PICTURE = enum.auto()
    TROPHIES = enum.auto()
    CERTIFICATE = enum.auto()
    BOOKS = enum.auto()
    RELICS = enum.auto()
